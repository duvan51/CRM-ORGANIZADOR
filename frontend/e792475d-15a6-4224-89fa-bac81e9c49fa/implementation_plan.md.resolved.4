# Plan de Columnas Dinámicas y Filtrado Avanzado

Este plan permite personalizar completamente qué datos consolidar y cómo explorarlos al finalizar.

## Cambios Propuestos

### [Backend - ETL]
#### [MODIFY] [etl.py](file:///c:/Users/duvan/OneDrive/Escritorio/proyectos/CRM-ORGANIZADOR/backend/app/etl.py)
- **Eliminar `COLUMNAS_REQUERIDAS` estático**: Ahora el procesado aceptará una lista de campos destino desde el frontend.
- **[procesar_archivos](file:///c:/Users/duvan/OneDrive/Escritorio/proyectos/CRM-ORGANIZADOR/backend/app/etl.py#66-154) flexible**: Iterará sobre los campos definidos en el mapeo sin importar cuántos sean.
- **Tratamiento de Fechas**: Se aplicará el parseo de fechas solo si el campo mapeado se llama exactamente "fecha".

### [Frontend - UI/UX]
#### [MODIFY] [App.jsx](file:///c:/Users/duvan/OneDrive/Escritorio/proyectos/CRM-ORGANIZADOR/frontend/src/App.jsx)
- **Gestión de Campos**: Añadir UI en el Paso 1/2 para definir qué campos queremos unificar (Ej: "Nombre", "ID", "Saldo").
- **Mapeo Dinámico**: El Paso 2 generará automáticamente selectores para cada uno de estos campos por cada hoja.
- **Barra de Búsqueda (Paso 3)**: Añadir un input de búsqueda que filtre la tabla de resultados en tiempo real por cualquier columna.

## Plan de Verificación
1. **Dinamismo**: Agregar un campo "Teléfono", mapearlo y verificar que aparezca en la tabla final.
2. **Filtrado**: Escribir el nombre de un paciente en la barra de búsqueda y ver que la tabla solo muestre sus registros.
3. **Persistencia**: Verificar que si no se mapea un campo opcional, el proceso continúe (o el frontend lo valide).
